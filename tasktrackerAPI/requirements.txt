# --- Core framework ---
Django>=5.0,<6.0

# --- API layer ---
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0

# --- Heroku / production ---
gunicorn>=21.2,<22.0
whitenoise[brotli]>=6.6,<7.0  # serve static files with DEBUG=False

# --- Database (Heroku Postgres) ---
psycopg2-binary>=2.9.9,<3.0
dj-database-url>=2.2,<3.0

# --- Optional (useful locally; not required on Heroku) ---
# python-dotenv>=1.0,<2.0
